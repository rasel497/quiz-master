import React from 'react';
import QuizBar from '../QuizBar/QuizBar';

const Topics = ({ quiz }) => {
    // const { id, total } = quiz;
    return (
        <div>

        </div>
    );
};

export default Topics;